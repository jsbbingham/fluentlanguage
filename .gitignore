# Environment / secrets
.env
env.php
config/env.php

# Data files (server-side only) — anchored so src/data/ stays tracked
/data/
*.log

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/

# Old config (not needed in repo)
fluentlanguage.net.conf

# Build output / deps
node_modules/
dist/

# Audit tool config (squirrelscan)
squirrel.toml
.squirrel/
